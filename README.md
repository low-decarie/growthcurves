Growth curve fitting functions recovering ecologically relavant parameters

install package using:

```
if(!require(devtools)){install.packages("devtools")
                       require(devtools)}
                       
```
